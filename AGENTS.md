SpotifyMatcher is a tool for migrating a local library of music to Spotify.

Tests should be exclusively black-box: verify behavior and outcomes, not
implementation details.

Use the repository-local `.venv` for Python commands whenever it exists. Do not
install dependencies into the global/user Python environment for this project;
activate `.venv` or invoke tools through `.venv/bin/python` instead.
